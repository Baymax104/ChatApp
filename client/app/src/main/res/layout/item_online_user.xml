<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
                name="user"
                type="com.baymax104.chatapp.entity.User" />
        <variable
                name="itemClick"
                type="com.baymax104.basemvvm.view.BaseAdapter.ListHandlerFactory.OnItemClickListener" />
    </data>

    <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="70dp"
            android:paddingHorizontal="10dp"
            android:layout_marginVertical="5dp"
            android:onClick="@{v->itemClick.onClick(user, v)}">
        <TextView
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:text="@{user.username}"
                android:gravity="center_vertical"
                android:textStyle="bold"
                android:textSize="17sp"
                android:textColor="@color/black"
                android:fontFamily="@font/normal"/>

        <TextView
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:text="@{user.gender + ` ` + ((!user.age.equals(``)) ? user.age + `岁` : ``)}"
                android:layout_gravity="center_vertical"
                android:textSize="15sp"
                android:textColor="@color/gray_1"
                android:fontFamily="@font/normal"
                android:gravity="center_vertical"/>

    </LinearLayout>
</layout>